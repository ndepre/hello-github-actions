jobs :
   build :
     nom : l' action Hello world 
    s'exécute : ubuntu-dernières 
    étapes :
      - utilise : actions / checkout @ v1 
      - utilise : ./action-a 
        avec :
           MY_NAME : " Mona "
